<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title><%=title%></title>
	</head>
	<body>
		<% extend("layout") %>
		<h1><%=title%>: <%=bookinstance._id%></h1>
		<p>
			<strong>Title: </strong>
			<a href="<%=bookinstance.book.url%>"
				><%=bookinstance.book.title%></a
			>
		</p>
		<div style="margin-left: 20px; margin-top: 20px">
			<h4>Details:</h4>
			<p>
				<strong> Imprint: </strong>
				<%=bookinstance.imprint%>
			</p>
			<p>
				<strong>Status: </strong>
				<%if(bookinstance.status=="Available"){%>
				<span style="color: green"> <%=bookinstance.status%></span>
				<%}else if(bookinstance.status=="Maintenance"){%>
				<span style="color: yellow"><%=bookinstance.status%></span>
				<%}else{%>
				<span style="color: red"><%=bookinstance.status%></span>
				<%}%>
			</p>
			<%if(bookinstance.status!="Available"){%>
			<p>
				<strong>Due back: <%=bookinstance.due_back_formatted%></strong>
			</p>
			<%}%>
		</div>
		<p>Do you really want to delete this book instance?</p>
		<form action="" method="post">
			<div class="form-group">
				<input
					type="hidden"
					id="bookinstanceid"
					class="form-control"
					name="bookinstanceid"
					value="<%=bookinstance._id%>"
				/>
			</div>
			<button class="btn btn-primary" type="submit">Delete</button>
		</form>
	</body>
</html>
